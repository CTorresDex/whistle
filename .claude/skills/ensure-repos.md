Reads  `project.pug`

- Ensure every repository is cloned at repositories/{repository.name} from repository.url at the commit stated at repository.commit, if the commit is not provided, keep it the latest